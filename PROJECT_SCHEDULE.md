# Rhythmiq Project Schedule

## Objective

当前项目按单用户模式推进，不做 `User` 相关能力，默认只有一个固定使用者。

这个产品要解决的不是简单的待办记录，而是更贴近你真实工作流的需求：

- 你有固定作息和固定不可安排时间
- 你有重复习惯，例如晚间锻炼
- 你有工作任务、学习任务和个人项目并行推进
- 你希望 LLM 不只是“生成一张表”，而是理解背景、补全产出、提醒未定项、并帮助做次日规划

因此当前项目应按以下路线推进：

1. V1：规则驱动的次日规划 MVP
2. V2：任务管理接入规划
3. V3：Todo 软件 API 集成

排期原则：

- 前后端排在一起推进，不拆成两条独立线
- 每个阶段都必须形成可见页面和真实可操作结果
- 所见即所得，优先先做闭环，再做扩展

## Requirement Review

基于你给出的样例，系统输入本质上应统一为两大类：硬约束与软约束。

### Hard Constraints

硬约束是必须优先满足、不可被普通任务覆盖的时间规则。

#### Fixed Schedule

- 工作日起床时间：`08:40`
- 到达公司时间：约 `09:20`
- 午休时间：`13:10-13:40`
- 回到公司时间：约 `14:10`
- 下班时间：`18:00`
- 到家时间：`20:00`
- 睡觉时间：`01:00`

#### Repeating Habits

- 晚上哑铃：20 分钟
- 晚上练腿：20 分钟

结论：

- `Fixed Schedule` 和 `Repeating Habits` 本质上都属于硬约束
- 它们只是在来源上不同：一个是固定作息，一个是重复习惯
- 在数据模型上可以继承同一个 `HardConstraint` 概念，只用不同 `type` 区分

### Soft Constraints

软约束不是绝对锁死的时间块，而是参与规划排序、解释和分配的上下文信息。

它们本质上可以继承同一个 `SoftConstraint` 概念，只是打上不同 `tag`。

#### Learning Tag

- 最近在学习 DDIA
- 当前进度：看到“批处理系统”
- 剩余进度：还有三章

#### Work Tag

- 上午要产出规则检测排期文档
- 下午发给 MT 沟通
- `15:00-16:00` 开周会，与 LD 同步
- `16:00-18:00` 搞懂一个规则，并考虑规则调试设计

#### Personal Project Tag

- 最近在做 Todo 软件
- 还存在“知识树构建规划”这类未明确排班时间的事项
- 若 Todo 一周内完成，则第二周晚间时间切到知识树项目

结论：

- `Learning Context`、`Work Context`、`Personal Project Context` 本质上都是软约束
- 它们的主要区别不在结构，而在标签、优先级、预估时长、时间偏好和产出要求
- 因此数据模型上更适合统一成 `SoftConstraint` 或 `PlanningItem`，再通过 `tag` 标识为 `learning`、`work`、`project`

## Product Direction

### V1: Next-Day Planner MVP

核心目标：

让系统根据固定作息、重复习惯和少量手动输入，生成一份可信的“第二天规划”。

核心流程：

配置固定规则 -> 补充当天上下文 -> 点击生成 -> 查看第二天规划 -> 晚间复盘并查看次日计划

这个版本不是自由任务管理器，而是“规则驱动的次日规划器”。

### V2: Task Management

核心目标：

让规划结果不仅依赖习惯，还依赖任务、项目和学习进度。

核心流程：

管理任务/项目 -> 参与规划生成 -> 生成更精细的第二天规划

### V3: Todo API Integration

核心目标：

把系统内生成的计划同步到外部 Todo 软件，实现真正的日程联动。

核心流程：

生成规划 -> 选择同步 -> 写入 Todo 软件 -> 回显同步结果

## Scope By Version

### V1 In Scope

- 单用户模式
- 固定作息配置
- 固定不可安排时间段
- 重复习惯配置
- 少量上下文输入
- 调用规划 API
- 生成第二天规划
- 在页面中展示规划结果
- 为任务块自动补全预期产出
- 对未定时事项给出提醒规划建议

### V1 Out of Scope

- 用户系统
- 登录鉴权
- 完整任务管理系统
- 多日连续排程优化
- 日历双向同步
- 推送通知
- 多用户协作

### V2 In Scope

- 任务创建、编辑、删除、完成
- 项目归属
- 优先级
- 预估时长
- 截止时间
- 学习进度管理
- 任务参与第二天规划生成

### V3 In Scope

- 接入一个 Todo 软件 API
- 将规划结果映射为待办或日程项
- 展示同步状态
- 为后续双向同步保留接口

## Feasibility Conclusion

这个需求是可行的，但实现方式不能是“纯 LLM 自动排班”，而应该是“规则引擎 + LLM”。

推荐分工如下：

- 规则层负责硬约束
- LLM 层负责理解上下文、补全产出、生成说明、做柔性安排

### Rule Layer Responsibilities

- 不能把任务排进通勤时间
- 不能把任务排进午休和回家路上
- 固定习惯必须占位
- 固定会议时间必须锁定
- 晚间复盘时间必须保留
- 先放置所有 `HardConstraint`
- 再为 `SoftConstraint` 留出可用时间窗口

### LLM Layer Responsibilities

- 理解任务背景
- 为任务自动补全“产出物”
- 为学习块补全“学习产出”
- 为未明确时间的事项生成提醒内容
- 在满足规则前提下组织自然语言解释
- 根据 `tag`、优先级、上下文和剩余时长对 `SoftConstraint` 做柔性排序

## Example Planning Target

按你给出的样例，一个合理的次日规划结果应该接近：

| Time | Plan | Notes |
|------|------|-------|
| 08:40-09:20 | 通勤 | 不安排任务 |
| 09:20-11:40 | 产出规则检测排期文档 | 需要包含背景、优先级、产出、预估 |
| 12:50-13:10 | 复习前一天 DDIA | 快速回顾关键概念 |
| 15:00-16:00 | 周会，与 LD 同步 | 固定会议时间 |
| 16:00-18:00 | 搞懂一个规则并设计调试思路 | 输出规则理解和调试方案 |
| 20:00-21:00 | 锻炼 | 哑铃 20 分钟 + 练腿 20 分钟 |
| 21:00-22:00 | 看完一章 DDIA | 自动补全学习产出 |
| 22:00-24:00 | 做 Todo 应用 | 自动补全当日产出 |
| 00:00-01:00 | 复盘今天并查看第二天计划 | 若知识树事项未排期，在此提醒规划 |

这张结果表说明，V1 不能只做“习惯配置”，还必须支持：

- 固定时间块
- 灵活任务块
- 产出物自动补全
- 未定项提醒

## Version Plan

### Phase 0: Planning

目标：冻结 V1 的最小闭环，避免产品方向继续漂移。

输出：

- 固定作息字段
- 重复习惯字段
- 灵活任务输入字段
- 次日规划结果结构
- 规则层与 LLM 层边界
- 页面草图

完成标准：

- 明确 V1 不是任务系统
- 明确 V1 的结果是“第二天规划”
- 明确哪些时间段是硬约束

### Phase 1: V1 MVP

目标：做出第一个真实可用的“次日规划器”。

用户可见能力：

- 配置固定作息
- 配置重复习惯
- 输入当天/近期上下文
- 点击生成第二天规划
- 查看规划结果及每段任务的说明和产出

开发范围：

- 作息配置数据模型
- 习惯配置数据模型
- 次日规划结果数据模型
- 规则排班逻辑
- LLM 规划生成接口
- 规划结果展示页面
- 基础错误处理与校验

完成标准：

- 可以保存作息和习惯
- 可以根据输入生成第二天规划
- 规划不会占用硬约束时间
- 页面中能看到每段计划的目标和产出

### Phase 2: V2 Task Management

目标：让规划真正跟任务和项目状态联动。

用户可见能力：

- 创建和编辑任务
- 设置优先级、预估时长、截止时间
- 管理项目，例如 Todo、知识树
- 让任务参与第二天规划生成

开发范围：

- Task / Project 数据模型
- 任务管理页面
- 任务 CRUD API
- 学习进度记录
- 规划逻辑接入任务和项目数据

完成标准：

- 规划结果会参考任务优先级和时长
- Todo 完成后可以把时间切换给知识树项目
- 页面可区分固定习惯块和任务块

### Phase 3: V3 Todo API Integration

目标：把内部规划结果推到外部 Todo 工具。

用户可见能力：

- 配置 Todo 软件连接信息
- 将计划同步到外部工具
- 查看同步结果

开发范围：

- 选择并接入一个 Todo API
- 规划结果到 Todo 数据结构映射
- 同步接口
- 前端同步入口和状态反馈

完成标准：

- 至少支持一个 Todo 软件
- 可将计划结果同步出去
- 失败时给出清晰错误提示

## Recommended Delivery Schedule

建议采用 4 周主线排期，先做可用闭环，再进入扩展阶段。

### Week 1

重点：

- 明确固定作息和重复习惯字段
- 明确次日规划结果结构
- 明确硬约束规则
- 完成 V1 页面草图

可见结果：

- 你可以确认“输入什么、输出什么、哪些时间不能动”

### Week 2

重点：

- 完成作息配置页
- 完成习惯配置页
- 完成基础规则排班逻辑
- 打通规划生成 API

可见结果：

- 页面可填写配置并请求生成规划

### Week 3

重点：

- 完成规划结果页面
- 增加 LLM 自动补全产出
- 增加未定项提醒
- 完成 V1 联调与修复

可见结果：

- 可以稳定得到第二天规划，并带有任务说明和产出

### Week 4

重点：

- 增加任务和项目基础模型
- 完成任务管理页面基础能力
- 为 Todo API 集成预留接口

可见结果：

- 进入 V2 起点，规划结果可开始接入任务数据

## Milestones

1. M1: 明确作息、习惯、上下文输入字段和规划结果结构
2. M2: 完成“输入规则 -> 生成第二天规划 -> 展示结果”的 V1 闭环
3. M3: 完成任务管理接入规划
4. M4: 完成 Todo 软件 API 集成

## Execution Priority

建议严格按下面顺序推进：

1. 先定义固定规则和结果结构
2. 再实现 V1 次日规划闭环
3. 再引入任务和项目管理
4. 最后接 Todo 软件 API

## Risks

- 一开始就做完整任务系统，导致 V1 迟迟无法上线
- 只做 LLM 生成，不做规则校验，导致排期结果不可信
- 结果结构定义不稳定，后续任务接入反复返工
- 过早接 Todo API，拖慢核心价值验证

## Working Table

下面这张表用于你直接查看排期并据此安排工作。

| Stage | Time | Goal | Key Work | Visible Deliverable | Exit Criteria |
|------|------|------|----------|---------------------|---------------|
| Phase 0 | Week 1 | 冻结 V1 范围 | 定义作息字段、习惯字段、上下文字段、规划结果结构、页面草图 | 明确输入输出和硬约束规则 | 已确认 V1 边界，不再继续扩需求 |
| V1 MVP | Week 2-3 | 做出次日规划闭环 | 作息/习惯配置、规则排班、生成 API、结果页、产出补全、未定项提醒 | 可生成并查看第二天规划 | 可以稳定生成可信排期，且页面可直接使用 |
| V2 Tasks | Week 4+ | 接入任务和项目 | 任务 CRUD、项目管理、学习进度、规划接入任务数据 | 规划结果开始体现任务和项目 | 任务优先级、时长、项目会影响规划 |
| V3 Todo API | After V2 | 对接外部 Todo 工具 | 选择 Todo API、数据映射、同步接口、状态反馈 | 规划结果可同步到外部工具 | 至少一个 Todo 软件可用 |

## Immediate Next Step

建议下一步直接补一份 `MVP_SPEC.md`，把 V1 拆成：

- 页面清单
- 表单字段
- 规划结果结构
- 规则引擎规则
- LLM 输入输出格式
- API 设计
